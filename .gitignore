# Rust build artifacts
/target
**/*.rs.bk

# Vendor reference documents — large, third-party, not redistributed with the
# source. Kept locally under docs/references/external/ (see external-references.md).
/docs/references/external/

# Editor / OS noise
.DS_Store
*.swp
